# 501 bug-schema import

After accessing TuGraph via port 7070 and exporting a schema, an error occurs when attempting to import with this schema, indicating that the terms "index" and "unique" are illegal.